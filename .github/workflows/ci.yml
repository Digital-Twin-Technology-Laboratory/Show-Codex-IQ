name: CI

on:
  push:
  pull_request:

jobs:
  test:
    # Liquid Glass APIs require the macOS 26 SDK even though the app keeps a
    # macOS 14 deployment target and provides runtime fallbacks there.
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Toolchain
        run: |
          xcodebuild -version
          swift --version

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate project
        run: xcodegen generate

      - name: Verify core
        run: swift run CoreVerification

      - name: Run Xcode tests
        run: |
          xcodebuild \
            -project ShowCodexIQ.xcodeproj \
            -scheme ShowCodexIQ \
            -destination 'platform=macOS' \
            ARCHS="$(uname -m)" \
            ONLY_ACTIVE_ARCH=YES \
            CODE_SIGNING_ALLOWED=NO \
            test
